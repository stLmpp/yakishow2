import { UniqueCelularDirective } from './unique-celular.directive';

describe('UniqueCelularDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueCelularDirective();
    expect(directive).toBeTruthy();
  });
});
