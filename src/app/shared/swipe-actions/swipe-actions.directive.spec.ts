import { SwipeActionsDirective } from './swipe-actions.directive';

describe('SwipeActionsDirective', () => {
  it('should create an instance', () => {
    const directive = new SwipeActionsDirective();
    expect(directive).toBeTruthy();
  });
});
