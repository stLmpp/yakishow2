import { HammerjsDirective } from './hammerjs.directive';

describe('HammerjsDirective', () => {
  it('should create an instance', () => {
    const directive = new HammerjsDirective();
    expect(directive).toBeTruthy();
  });
});
