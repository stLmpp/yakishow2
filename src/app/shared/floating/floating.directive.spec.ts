import { FabFloatingDirective } from './fab-floating.directive';

describe('FloatingDirective', () => {
  it('should create an instance', () => {
    const directive = new FabFloatingDirective();
    expect(directive).toBeTruthy();
  });
});
