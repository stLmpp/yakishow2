import { UniqueCodigoDirective } from './unique-codigo.directive';

describe('UniqueCodigoDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueCodigoDirective();
    expect(directive).toBeTruthy();
  });
});
